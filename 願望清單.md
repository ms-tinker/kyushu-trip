# 願望清單

> 這裡記錄問過、但目前還做不到、很難做、或先擱置的功能，方便之後回來翻。已經做好的
> 東西都寫在 `使用方法.md` 裡，這份只放「還沒有」的。

## 1. GCP 預算警報（$1 門檻）

**現況**：想在 Google Cloud 設定「花費超過 $1 就提醒」的預算警報，`gcloud billing budgets
create` 指令一直回傳 `INVALID_ARGUMENT` 400 錯誤，就算用 `--log-http` 看過完整的請求/
回應內容（格式看起來是對的）也沒找到確切原因。已經改用網頁上的「雲端空間用量進度條」
（見「總覽」分頁）替代，這個功能就先擱置了，不算緊急。

## 2. 每日行程自動排序（依路線順不順）

**想法**：從口袋清單拉了幾個景點/餐廳到某一天之後，該天已經有固定的起點/終點，希望能
依「路線順不順」自動幫中間的點排序。

**技術上可行**：Google 的 Directions API 有 `optimizeWaypoints` 參數——給定起點、終點、跟
中間一串途經點，會算出「開車距離/時間最短」的拜訪順序，跟現有查開車時間用的是同一套
API，不用另外接新服務。

**取捨/待討論**：
- 只考慮開車距離最短，不懂「這家店幾點開」「午餐該中午到」「寶寶這時間該睡覺」這些
  真實限制——直接套用可能排出「路線最短、但店還沒開門」這種不實用的結果
- 建議做成「建議排序」按鈕（算出來給你看、你確認要不要套用），不要自動默默改掉已經
  排好的順序
- 範圍可能只做「同一個時段內（例如上午）還沒排定順序的點」，起點/終點跟已經手動決定
  的時段分配不動
- 待確認：要不要把「停留時間」也一起考慮進去，排出來的不只是路線順序，還包含建議
  幾點到幾點
