# 願望清單

> 這裡記錄問過、但目前還做不到、很難做、或先擱置的功能，方便之後回來翻。已經做好的
> 東西都寫在 `使用方法.md` 裡，這份只放「還沒有」的。

## 1. 開車時長自動根據行程順序更新

**現況**：每個地點的「開車 20 分」是寫在 `time` 欄位裡的自由文字，跟停留時間混在一起
（例如「約 30 分・開車 20 分」），是手動填的。行程現在支援拖曳排序、跨天搬移，順序一動，
原本填的開車時間就可能不準了，但不會自動更新。

**為什麼還沒做**：
- 要先把「停留時間」跟「開車時間」拆成兩個獨立欄位（現在是同一格自由文字，沒辦法只
  自動更新其中一半，也會影響到已經手動填好的所有紀錄格式）
- 要接 Google Maps Directions API，每次順序異動後，對當天所有有經緯度的地點重新算一次
  「跟上一站差幾分鐘車程」
- 技術上做得到（Directions API 有免費額度），但架構改動不小，值得另外找時間做，不要
  跟其他小改動混在一起。

## 2. Google My Maps 即時自動同步

**背景**：口袋清單的地點目前是手動匯出 KML、給 Claude 匯入的（見 `使用方法.md`）。原本
想要的是「在 My Maps 上新增/刪除/編輯，直接反映到網站」。

**為什麼做不到（完全照你想的那樣）**：
- Google My Maps 沒有官方 API，也沒有「異動就通知」的機制
- 有一個非官方但很多人用的公開 KML 網址，測試過，因為地圖目前共用設定不是公開的，
  回應是 403（打不通）
- 就算把地圖設成「知道連結的人都能看」，做得到的也只是「每次打開網站時重新抓一次
  My Maps 目前的內容」，不是你在 My Maps 按存檔那一刻就反映過來
- 目前選擇：維持手動匯出 KML、請 Claude 重新匯入的做法

**如果之後想要更自動一點**：把地圖分享設定改成「知道連結的人可看」，可以做「每次開網站
自動重新抓最新版」，但地圖上所有標記點對外都會是可查看的（等於半公開）。

## 3. 口袋清單接上雲端同步

**現況**：口袋清單目前存在瀏覽器記憶體裡（沒有寫進 Firestore），重新整理頁面會回到
上次匯入 KML 當下的狀態，兩人手機也看不到彼此對口袋清單的異動（例如你手機上刪掉一個
點，另一半手機上不會消失）。「加入行程」這個動作本身是真的雲端同步的，只是口袋清單
本身還不是。

**為什麼還沒做**：這是先求「看樣子、試互動」故意做成本機版，避免測試過程中把示範資料
寫進兩人共用的雲端資料庫。等口袋清單的介面/互動確定沒問題之後，要接上雲端同步（用跟
行程/記帳/清單一樣的 Firestore 即時同步方式）是下一步，還沒開始做。

## 4. GCP 預算警報（$1 門檻）

**現況**：想在 Google Cloud 設定「花費超過 $1 就提醒」的預算警報，`gcloud billing budgets
create` 指令一直回傳 `INVALID_ARGUMENT` 400 錯誤，就算用 `--log-http` 看過完整的請求/
回應內容（格式看起來是對的）也沒找到確切原因。已經改用網頁上的「雲端空間用量進度條」
（見「總覽」分頁）替代，這個功能就先擱置了，不算緊急。
