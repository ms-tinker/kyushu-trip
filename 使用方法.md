# 九州親子自駕行程 App — 使用方法

> 這份檔案是整個 app 的說明文件。之後每次有功能更新、部署，都會一併更新這裡的內容，
> 讓你隨時能回來看目前有哪些功能、怎麼用。還沒做到／做不到的事記在 `願望清單.md`。

## 這是什麼

給 8/26–9/1 2026 九州親子自駕（5 個月大寶寶）用的行程規劃網站。手機瀏覽器打開即可，
不用安裝 app。兩人的手機打開同一個網址，行程、記帳、清單、口袋清單、連結都會即時同步。

**網址**：https://ms-tinker.github.io/kyushu-trip/

視覺風格是「墨韻印章」：卡片頂部粗色條、標題實心色帶，每個地點/紀錄的顏色跟著它所屬的
日期走（8/26=靛藍、8/27=土黃、8/28=墨綠、8/29=磚橘、8/30=靛藍、8/31=土黃、9/1=灰）；
清單的待辦/打包/連結各自也有自己的顏色（土黃／墨綠／莫蘭迪紫）。

## 六個分頁

### 總覽
- 快速跳轉列 + 可拖曳排序的區塊（航班、住宿、每日行程摘要、天氣、地圖、雲端空間用量）
- 整趟行程的總覽地圖，會隨著「行程」分頁裡的編輯即時更新
- 航班卡片有標註桃園（TPE）航廈（目前是第1航廈，這是虎航 Tigerair Taiwan 目前的資訊，
  建議出發前再跟訂位確認信或官網核對一次，怕航廈臨時調整）
- 天氣卡片（福岡／由布院／阿蘇）都有「每日／每小時」切換：每小時模式會先選一天，再看
  那天 24 小時的溫度/天氣圖示/降雨機率；「行程」分頁裡每天的小地圖下面、起點卡片上面
  也放了一份對應那天地點的逐時天氣（例如 8/29 阿蘇景觀自駕日會顯示阿蘇的逐時天氣）

### 行程
- 日期列鎖在畫面上方，往下滑不會消失；最前面多一顆「🧺 口袋」可以切到口袋清單
- **這天的摘要**：每天最上面有一塊色塊，用當天的代表色（淺色背景＋深色上緣線）、粗體
  文字，一眼就看得出來是這天的重點摘要；點右上角的 ✎ 可以直接編輯、存檔，沒有內容時
  會顯示「點右上角 ✎ 新增這天的重點摘要…」的提示文字。9/1 這種標了警示的天（退房/
  航班時間很緊）固定顯示紅橘色警示樣式，不會跟著換成當天代表色，這是刻意保留的例外
- 每天分成「起點／早餐／上午／午餐／下午／晚餐／晚上／終點」幾個時段
- 每個地點可以編輯、刪除、新增、上傳照片（兩人都能傳，**除了選檔案，也可以直接複製
  一張圖片後在編輯表單裡貼上**，不用先存成檔案再選），地點名稱搜尋用 Google Places，
  強制抓日文結果（避免搜尋建議跟選完之後的名稱語言不一致）
  - ⚠️ 上傳/貼上照片、或編輯過程中背景查到營業時間等資料時，**編輯表單不會被打斷**：
    之前這些背景動作會觸發整個畫面重畫，把正在編輯、還沒存檔的表單換成唯讀卡片，
    等於編輯到一半突然被關掉；現在編輯表單開著的時候，這些背景更新會先暫停套用，
    等你按「儲存」或「取消」離開編輯才會套用最新資料
- **備註支援換行**：在備註欄位裡按 Enter 換行，卡片上顯示也會照著換行，不會被擠成一行
- **拖曳排序**：卡片左上角抓取圖示，按住可以拖到同一天的其他時段；跨天搬移不用拖曳，
  在編輯表單裡選「搬到其他天／時段」（按鈕列，不是下拉選單）
- 每個地點都有導航連結（自動帶入上一站當起點）、Mapcode 查詢連結、經緯度
- **點地點名稱**會用新分頁打開那個地點的 Google Map 頁面（不是導航，是看照片/評論用
  的地點頁面），跟口袋清單的名稱連結是同一套邏輯，同樣有可能遇到少數名稱查到「部分
  相符的結果」列表要再點一次的情況（見口袋清單那段的說明）
- 每天下方有小地圖，標記點都有數字編號，跟下面卡片名稱旁邊的同色數字圓章對應——**地圖
  跟清單互動**：點卡片上的編號圓圈，地圖會自動 pan 過去、跳出那個點的資訊視窗，還會用
  彈跳動畫強調一下；點地圖上的標記點，下面清單會自動捲到對應的卡片並且短暫用外框強調
  （跟口袋清單的地圖互動是同一套邏輯）
- **營業時間**：地點編輯表單裡重新搜尋、選過地點的話，會自動抓 Google 目前的每週固定
  營業時間，卡片上依這個地點所在的行程日期（例如 8/29）算出星期幾，顯示那天的營業時間
  或「本日公休」；沒有透過搜尋選過的地點（舊資料、或從口袋清單加進來的）會在每次打開
  這天的時候自動背景補查一次，查過一次就不會再重查（避免每次開頁面都重打 API）
- **開車時間／走路時間**：存/加地點時會自動用 Google 路線服務算「跟上一站的開車時間」
  （**有考慮當下路況**，儘量貼近你按「導航」會看到的數字，不是完全沒路況的理論值），
  顯示在名稱旁邊的色塊，格式固定是「開車 X，重點文字」；如果開車時間很短（10 分鐘以內，
  通常代表距離不遠），會順便查一次走路時間，走路在 20 分鐘以內的話色塊會自動改顯示
  「步行 X」取代「開車 X」（比開車實際，不用找停車位）——這兩個門檻是寫死在程式裡的
  經驗值，之後想改的話跟我說。「重點」那段是你自己填的自由文字，編輯表單裡的欄位叫
  「重點」（原本叫「停留時間」，內容不一定是時間長度，會自動加「停留」兩個字的行為
  也拿掉了，現在你打什麼，色塊上就直接顯示什麼，例如「記得買門票」也可以）
- **搬動/重新排序地點之後，開車/走路時間跟營業時間都不用手動處理**：
  - 開車/走路時間是「跟上一站的關係」，不是這個地點自己的固定屬性——不管是同一天內
    拖曳排序、還是用編輯表單「搬到其他天／時段」換了日期，只要下次打開受影響的那天，
    系統都會**自動抓出現在真正的上一站**、判斷跟上次算的時候是不是同一個，不一樣就
    自動重算，不用手動按刷新
  - 營業時間本身存的是「這個地點一整週、每天幾點到幾點」的完整資料，不是只存單一天，
    所以把地點搬到別天，畫面只是照新的那天重新算一次是星期幾去對照原本就有的資料，
    不需要、也不會重新查詢 Google
  - 例外：如果你是編輯表單裡**重新搜尋、換了地點本身**（不是搬到別天，是換成別的
    Google 地點），開車時間會自動清空重算，營業時間會用重新搜尋抓到的最新資料
  - 一天裡**最前面那個有座標的地點（通常是「起點」）不應該顯示開車時間**——如果它是
    「後來才變成第一個」的（例如原本前面還有別的地點，後來被搬走/刪掉），系統會自動
    偵測到「這個地點前面已經沒有別的地點了」，把卡在上面的舊開車/走路時間清掉，不用
    手動處理
- 日期標題旁邊有一個「🔄 重新查這天」小按鈕，平常用不到（新增/進入頁面都會自動查、
  前一站變了也會自動查），只有想「強制重新確認一次」（例如查詢失敗過、或剛啟用某個
  Google Cloud API、或想讓已經算過的開車時間套用最新的路況考量邏輯）才需要按，按了會
  忽略所有快取、連已經有數字的也重查一次，並直接在下面顯示查詢結果

### 口袋清單（行程分頁裡的一個切換頁）
- **直接跟你的 My Maps 即時同步**：每次打開網站，背景會自動抓一次 My Maps 目前的內容，
  抓成功就整批更新（依圖層/Folder 分組，分類是依 KML 裡的圖示顏色自動判斷成觀光/購物/
  餐廳/交通/住宿），兩支手機都看得到同一份最新版——不用再手動匯出 KML 請 Claude 幫忙
  匯入了
- **篩選：地區／分類／標籤／星等都支援多選**，點一下加選、再點一下取消，跟「全部」是
  互斥的（點「全部」＝清空這個維度的篩選，不特別限制）。同一個維度內是「符合其中一個
  就算」（例如地區選「福岡市＋阿蘇市」＝這兩個地區的都會出現），四個維度之間是「都要
  符合」（例如同時選了地區「福岡市」跟分類「餐廳」＝只顯示福岡市裡的餐廳）
  - 只選一個地區時，畫面跟以前一樣：地圖只顯示那個地區，卡片也不會多餘的地區標籤
  - **選了兩個以上地區（或不選、等於全部地區）時**，會把選到的地區合併成一份清單/地圖
    一起看，這時候每張卡片會多顯示一行「📍 地區名」，方便分辨這個點是哪個地區的
  - 沒有選分類/標籤/星等任何篩選時，畫面照分類分組、可收合，上面有「全部展開／全部
    收合」；只要選了其中任何一種，就會變成不分組的扁平清單
  - 分類／標籤／星等這三排篩選按鈕會**互相反映彼此目前的篩選結果**：例如選了「觀光」
    分類，下面的標籤列就只會列「觀光類地點」用過的標籤（不會還跳出餐廳的標籤），數字
    也是套用其他篩選之後的結果；反過來選標籤/星等，分類列的數字也會跟著變。地區（最
    上面那排）不受這三個影響，維持單純的地區分組
- **卡片名稱可以直接點開 Google Map**：點地點名稱（不是導航，是那個地點本身的 Google
  Map 頁面，看得到照片/評論），會用新分頁打開；名稱旁邊直接放星等、再放營業時間，不用
  往下捲。卡片上原本的「導航」「查 Mapcode」「經緯度」都拿掉了（口袋清單的地點通常還
  沒決定哪天要去、要從哪裡出發，導航意義不大；經緯度只是給查 Mapcode 用的輔助資訊，這兩
  個一起拿掉版面清爽很多）——真的要導航的話，等按「＋加入行程」排進行程之後，行程那邊
  的卡片就有完整的導航連結
  - ⚠️ **這個連結不是每次都 100% 準確跳到正確頁面**：因為沒有存 Google 的 place_id
    （KML 匯入、手動加的地點都沒有），只能用「名稱」去搜尋，多數地點會直接跳到正確
    頁面，但少數同名或名稱比較特殊的地點，可能會先跳到「好幾個部分相符的結果」的列表
    要你自己再點一次；如果名稱本身是你自己取的暱稱（不是 Google 地圖上真的登記的店名，
    例如「香蕉人」），則會直接查不到——這是沒有 place_id 時無法完全避免的限制
- **標籤**：自己定義的文字標籤（例如「寶寶友善」「室內」「雨天備案」）。編輯表單／新增
  地點的「標籤」是**點選式**的，不是打字：已經用過的標籤會列成按鈕，點一下就加上/取消，
  想要新標籤才用旁邊的輸入框新增——這樣同一個標籤字之後在別的卡片也能直接點，不用重打、
  也不會打錯字變成兩個很像但其實不同的標籤。卡片上會顯示成小標籤。這個圖層裡只要有人
  用過的標籤，上面會自動多一排標籤篩選按鈕（沒有標籤的話這排就不顯示）
- **想去的程度**：1～3 顆星（2026-08-16 從 5 星改成 3 星），卡片上直接點星星就能打分數
  （點目前最亮的那顆會退回一級，可以清到 0 顆）。篩選按鈕是「剛好幾顆星」（不是「幾顆星
  以上」），而且可以多選，例如同時選 3 星跟 1 星，就會顯示「剛好 3 星」或「剛好 1 星」的
  地點，2 星不會出現。⚠️ 改成 3 星之前打過 4／5 星的舊資料，已經自動壓成新的滿分 3 星
  （原本「很想去」的排序意義不變）
- **營業時間**：跟「行程」分頁同一套邏輯——每個地點只查一次 Google 的營業時間資料，
  查過就存起來，之後不會重複打 API。卡片上顯示的是「今天」（你瀏覽的當下這個真實日期）
  的營業時間，例如「今日營業 09:00–18:00」或「今日公休」；因為口袋清單裡的地點還沒排進
  行程的哪一天，沒辦法像行程頁那樣照排定日期算星期幾，只能先給「現在去大概開不開」的
  參考。等真的按「＋加入行程」排進某一天之後，行程那邊會照排定的那天重新查一次準確的
  營業時間，不會沿用口袋清單這裡算出來的
  - 地區篩選上面有一個「🔄 重新查營業時間」小按鈕，會**強制**重新查一次「目前篩選到的
    地區」顯示的地點（忽略已經查過的快取；不篩地區的話就是全部地區）。平常不用按——
    沒查過的會自動查；只有想「換一天再確認一次」（例如剛好那天公休、或想確認週末營業
    時間）才需要手動按
- **地圖跟清單互動**：地圖顯示的地點會跟著上面所有篩選（地區/分類/標籤/星等）一起變動（篩選剩幾個
  點，地圖就只顯示那幾個）；點清單卡片上的編號圓圈，地圖會自動 pan 過去、跳出那個點的
  資訊視窗，還會用彈跳動畫強調一下；點地圖上的標記點，下面清單會自動捲到對應的卡片並且
  短暫用外框強調。地圖上的編號固定跟卡片編號一致，不會因為篩選只剩幾個點就重新從 1 排
- 卡片可以編輯（含重新搜尋地點、改圖層、改類型、改標籤、改星等、改備註）、刪除、拖曳
  排序（只在同分類內移動）——這些手動異動裡，**標籤、想去的程度、查過的營業時間，還有
  你在編輯表單裡存過的名稱／類型／備註，都會保留到下次同步**（比對圖層+經緯度，帶過去），
  不會被 My Maps 最新內容蓋掉
  - 名稱／類型／備註這三個「My Maps 本來就有的欄位」預設還是照 KML 最新內容走（這樣
    你在 My Maps 上改的東西才看得到）——**只要你在這個網站的編輯表單裡存過一次，那個
    欄位就會固定用你存的版本，不會再被 My Maps 蓋掉**，直到你自己再改一次。目前沒有
    「改回去照著 My Maps 走」的按鈕，想恢復的話只能刪掉這個地點、等下次同步重新從
    KML 長出來（會是全新一筆，標籤/星等要重打）
- ⚠️ 標籤/星等/營業時間/你手動存過的名稱類型備註，保留都是靠「圖層+經緯度」配對舊資料，
  沒有真正的唯一 ID 可用（KML 本身沒有）——如果你之後在 My Maps 上**明顯搬動一個點的
  位置**，配對會對不起來，那個點上面所有這些額外資料都會重置，這是資料來源本身的限制
  （單純改名字不影響配對，配對本來就不看名字）
- 「＋新增口袋地點」是彈出視窗：填名稱（可以用 Google 搜尋帶出座標）、圖層（一定要選，
  對應 My Maps 上的哪個資料夾）、類型、標籤、星等、備註，按「新增」才會真的加進清單
- 卡片上的「＋加入行程」會跳出一個小視窗選日期/時段，按確認就會真的寫進那天的行程
- 已經被「加入行程」過的地點會多顯示一個小標籤提醒你，但「＋加入行程」按鈕還是保留、
  可以繼續按——機場、飯店這種地點本來就會在好幾天分別當起點/終點重複出現，之前這裡
  曾經「加過一次就整個拿掉按鈕」，導致這類地點沒辦法加到第二天，是真的 bug，已經修掉
  - **名稱/備註會跟「最新一次」加入的那張行程卡片保持同步**：卡片上顯示「🔗 加入的
    日期」代表現在連結著哪一天；之後不管是在口袋清單改名稱/備註、還是直接去那張行程
    卡片改名稱/備註，兩邊都會互相同步。只連結最新一次，之前加過的舊卡片不會被牽動
    （例如機場先當第一天起點、後來又加到最後一天當終點，兩張卡片的備註可以各自不同、
    只有最後加的那張會繼續跟口袋清單同步）
  - 標籤是「已加入過但沒有連結」代表：這個地點跟行程裡某個地方座標一樣，但不是用
    「＋加入行程」這個按鈕加的（例如直接在行程手動輸入同一個地點），這種不會同步
  - ⚠️ 這個功能是新加的，只對**之後**用「＋加入行程」新增的卡片有效——如果你在這個
    功能上線前就已經加過的地點，沒有自動補上連結，想要同步的話重新按一次「＋加入
    行程」即可（會自動連結到這次新加的卡片，並取代原本的連結）
- ⚠️ **抓不到最新版的時候（網路問題、或你之後把 My Maps 改回私人），畫面會停在「上一次
  同步成功」的樣子**，不會空白或報錯——所以旅遊結束後可以放心把 My Maps 分享權限改回
  私人，口袋清單會定格在改回私人前最後一次同步的內容，不影響任何已經看過/加入行程的
  資料

### 記帳
- 一進來就是明細畫面（總花費 + 依類別圓餅圖、可收合的日期清單、起始現金餘額），上方
  「＋ 新增花費」按鈕會跳出彈窗填新紀錄，不會擋住底下明細
- 新增表單：日期（預設抓今天，不在範圍內就選 8/26）→ 類別 → 金額（支援加減乘除，跟
  幣別 JPY/TWD 同一行）→ 支付方式 → 品項 → 地點 → 收據照片 → 匯率（可手動覆蓋，自動
  抓當天最新 JPY→TWD、JPY→CNY，每筆紀錄都存了當時用的匯率）
- 類別、支付方式都有手繪風格的彩色小 icon（跟副食品 app 同一套畫法）
- 明細依日期分組（可收合），每組再依類別排序（類別排序跟著新增表單目前的順序走），日期
  旁邊直接顯示 `JPY ¥X = NT$X = CN¥X`；金額同時顯示 TWD 和 CNY
- 類別、支付方式都能自己新增/排序/刪除（右上角「排序／刪除」）
- 刪除紀錄前會跳出確認視窗
- 點「編輯」改已存的紀錄時，日期／幣別／支付方式／類別也都是按鈕列，不是下拉選單

### 清單
「清單」裡有三個子分頁：待辦事項／打包清單／購物清單（各自用不同底色標籤區分）。

**待辦事項／打包清單**
- 都是依分類分組的可收合清單（分類標題大字+粗體+icon），也有「全部展開」「全部收合」；
  分類順序跟著新增表單走
- 每個項目左邊有拖曳排序的抓取圖示，可以在同分類內調整順序
- 打包清單額外有數量（跟著品項名稱一起顯示，例如「奶瓶 x4」，數量是 1 就不特別顯示）
  跟裝箱功能，箱子名稱可以自訂
- 分類（含圖示）都能自己新增/排序/刪除；刪除項目前會跳出確認視窗
- 點「編輯」改已存的項目時，類別（打包清單還有箱子）也都是按鈕列，不是下拉選單

**購物清單**
- 這是仿 Notion 資料庫的呈現方式，**看板（Board）／表格（Table）兩種檢視可以切換**：
  看板照「分類」分成好幾欄（跟你截圖參考的呈現方式一樣），每張卡片顯示封面圖（有上傳
  的話取第一張）、名稱、標籤、預算；表格是傳統一列一筆的清單，多一欄縮圖跟「買了嗎」
  勾選框可以直接點，不用進編輯
- ⚠️ **欄位是固定的**（名稱／分類／標籤／圖片／連結／預算／買了嗎／備註），不是像
  Notion 那樣可以自己新增任意型別的欄位（例如數字欄、日期欄）——這是討論過的取捨，
  完全自訂欄位類型的通用系統工程量大很多、風險也高，固定欄位沿用app裡其他地方已經
  蓋好的元件（分類管理、標籤多選、圖片上傳），比較穩
- 「＋新增購物項目」是彈出視窗，同一個視窗也用來編輯已存在的項目（點看板卡片、或表格
  的名稱都會打開）；視窗裡有「刪除這個項目」按鈕（只有編輯已存項目時才會出現）
- 分類用的是跟記帳/待辦/打包同一套「可自己新增/排序/刪除」的分類管理元件，看板就是
  照這個分類分欄，預設分類是：藥妝／伴手禮／嬰兒用品／超市／服飾／3C家電／其他
- 標籤是點選式多選（點已有的標籤，或輸入新的），邏輯跟口袋清單的標籤一模一樣
- 圖片可以多張、兩人都能上傳，**除了選檔案，也可以直接複製一張圖片後在彈出視窗裡
  貼上**（Ctrl/Cmd+V）；如果上傳了照片但沒按儲存就把視窗關掉，剛上傳的那幾張照片會
  自動從雲端空間刪掉，不會留著佔用量
- 表格檢視的「買了嗎」勾選框可以直接點、不用進編輯視窗，看板檢視要打開卡片才能改

### 連結
- 最上面嵌入一份 My Maps 的即時地圖（跟 My Maps 網站上看到的一樣，會即時更新，但這個
  嵌入的地圖本身只能看，沒辦法跟口袋清單一樣有星等/加入行程這些互動功能）
- 下面是開車、查地點會用到的外部工具（例如 Mapcode 查詢），「＋新增連結」是彈出視窗
  的形式，可以自己新增／編輯／刪除
- 卡片只顯示「名稱」，不會把網址整串印出來占版面——**直接點名稱就會用新分頁打開那個
  網址**，不用另外找「開啟」按鈕
- 卡片左上角有抓取圖示，可以**手動拖曳排序**，跟行程/口袋清單/待辦/打包同一套操作方式
- 刪除前也會跳出確認視窗

> 記帳、待辦/打包清單、連結、口袋清單的「刪除前確認」都是同一個自訂彈窗（跟「新增花費」
> 同一套視覺風格），不是瀏覽器內建那個醜醜的 `confirm()` 對話框。

## 資料存在哪裡

- 行程、記帳、清單、照片、常用連結、口袋清單：都存在 Firebase（Firestore + Storage），
  兩支手機即時同步，不是存在瀏覽器本機
- 口袋清單存的是「最近一次從 My Maps 同步成功的內容」，跟其他資料一樣是雲端同步，但
  內容會被每次成功的 My Maps 同步整批取代（見上面口袋清單的說明）
- 總覽頁面有一個雲端空間用量的進度條，可以自己設定提醒的門檻

## 機票／住宿怎麼更新

這兩塊的基礎資料整理在同一層目錄的 `機票住宿行程.md`。以後要改班機時間、換飯店，直接
改那份 md 檔，改完跟 Claude 說一聲「同步到 GitHub」，就會幫忙把內容套進
`kyushu-itinerary.html` 並發布。

⚠️ **每日行程（景點）不再由這份 md 驅動**：2026-08-16 之前的做法是改 md 的「每日行程」
段落、同步時直接套回網頁（包含直接覆寫 Firestore 裡每天的資料），結果變成每次同步都會
把你自己在網頁上新增/編輯的地點蓋掉。已經把這個機制拿掉了——每天的實際地點請直接在
「行程」分頁上新增/編輯/刪除，`機票住宿行程.md` 只保留一張簡表當參考，不會再被拿來
同步覆寫。

## 口袋清單資料怎麼更新

不用手動操作了：每次打開網站，背景會自動抓一次 My Maps 目前的內容並整批更新口袋清單，
跟之前手動匯出 KML 請 Claude 匯入是同一套規則——KML（也就是 My Maps）裡刪掉的點，口袋
清單也會跟著消失；改名字的話會顯示新名字。**只要某個點已經被你「加入行程」過，那份就
是獨立副本，不會因為你之後在 My Maps 改東西而被回頭修改。**

這個自動同步的前提是 My Maps 的分享設定要是「知道連結的使用者」（不是私人）。旅遊
結束後想把地圖改回私人也沒關係，口袋清單會停在「改回私人前最後一次同步成功」的樣子，
不會空白，也不影響任何已經加入行程的資料（見上面口袋清單的說明）。

## 各種「自動更新」跟「刷新按鈕」是什麼邏輯

先分兩大類。**行程、記帳、清單、連結、口袋清單的名稱/圖層/類型/標籤/星等/備註**這些都是
直接存在 Firestore 裡的「你自己的資料」，不是查詢外部服務，本來就是**即時同步**（一支
手機改了，另一支手機幾乎立刻看到最新的），沒有「快取過期」這回事，也不需要刷新按鈕，
跟下面這張表完全是两回事。

下面講的是「要另外去問 Google/天氣服務」的資料，這幾種才有「查過一次就固定住，需不需要
手動重查」的問題。**核心結論先講在前面：正常情況下你完全不用手動按刷新，畫面上看到的
都已經是「當時能拿到的最新資料」；兩個 🔄 按鈕都只是「我不放心、想再確認一次」用的保險
選項，不是必需品。** 細節照「什麼時候會真的觸發一次新查詢」分三組：

**A 組：每次重新整理／打開這個網頁時，都會重抓一次（同一次瀏覽期間、沒重新整理的話不會
再抓第二次）**

| 資料 | 觸發時機 | 要花錢嗎 |
|---|---|---|
| 天氣（總覽＋行程頁逐時天氣） | 網頁載入時抓一次；之後你切換日期/分頁看到的都是**這一次**抓到的資料，不會每切一次就重抓 | 不用（Open-Meteo，免費） |
| 口袋清單地點本身（My Maps KML 同步） | 網頁載入時抓一次；之後同一次瀏覽期間不會再自動重抓（除非你自己編輯/新增/刪除口袋清單項目，那會另外觸發一次寫入，見下方） | 不用（純檔案下載，不算 Maps Platform 計費） |

**B 組：「這一天的畫面」每次重新畫出來時都會檢查一次，但只有「這個地點真的還沒查過」或
「查的條件變了」才會真的打 API，已經查過而且沒過期的會直接跳過**——所謂「這一天的畫面
重新畫出來」包含：切換到那一天的分頁、從別的分頁切回來、新增/編輯/刪除/拖曳排序這一天
的任何地點

| 資料 | 什麼情況算「查過但過期，要重查」 | 要花錢嗎 |
|---|---|---|
| 行程地點的營業時間 | 只有「從沒查過」這一種情況會重查；查過一次之後**永久不會自動重查**（除非你重新搜尋這個地點，或按強制重查按鈕） | 要（Places API） |
| 行程地點的開車時間 | 「從沒查過」，或**「跟上次算的時候比，前一站已經不是同一個了」**（前一站被搬動/刪除/整個行程重新排序過，包括「這個地點變成當天第一個地點」這種情況）——這兩種都會自動重查，不用按按鈕 | 要（Directions API，查詢時會考慮當下路況） |
| 行程地點的走路時間 | 條件跟開車時間綁在一起：只有開車時間本身 ≤10 分鐘時才會順便查一次（大多數相鄰地點距離較遠，開車都超過 10 分鐘，根本不會觸發） | 要（Directions API） |

**C 組：不是「你點進哪個分頁」觸發，是「口袋清單這份資料本身被寫入 Firestore 的那個
瞬間」觸發**——網頁載入時算一次寫入；之後你自己編輯/新增/刪除口袋清單項目、點星等/標籤、
拖曳排序、或 KML 同步抓到新版，都各自算一次寫入，都會再跑一次檢查（一樣是「沒查過的才
真的打 API」）。**單純反覆切換到「口袋」分頁、不做任何操作，不會產生新的查詢**——但也不
需要，因為第一次載入網頁時能查的都已經查過了

| 資料 | 什麼情況算「查過但過期，要重查」 | 要花錢嗎 |
|---|---|---|
| 口袋清單地點的營業時間 | 只有「從沒查過」這一種情況會重查，查過一次永久不會自動重查（除非按強制重查按鈕）。**顯示的文字本身會每天不一樣**：存的是「一整週、每天幾點到幾點」的完整資料，畫面每次重畫都會照「你瀏覽當下的真實日期」重新對照一次算出「今日」文字，這個對照不用重新查詢 Google，所以就算沒有新查詢，過幾天再看，「今日營業/今日公休」的文字也會自動換成新的那一天 | 要（Places API） |

兩個手動按鈕（行程頁「🔄 重新查這天」、口袋清單「🔄 重新查營業時間」）用途都一樣：忽略
上面講的所有「已經查過就跳過」規則，強制重新問一次 Google——用在「查詢曾經失敗過」「剛
啟用某個 Google Cloud API」「查到的剛好是公休想換一天再看看」這幾種情況，平常不需要按。

兩個按鈕都是「平常用不到」的設計：正常情況資料都會自動補齊，只有想「強制重新確認」時
才需要按（例如剛好那天查到公休想換一天再看看、剛啟用某個 Google Cloud API 想讓之前
查詢失敗的重試一次、或想讓已經算過的開車時間套用最新的路況邏輯）。

## Google Maps API 費用怎麼算、去哪裡看用量

**背景**：「行程」跟「口袋清單」分頁的地點編輯表單重新搜尋地點時，會多抓一個
`opening_hours`（營業時間）欄位；行程分頁存/加地點時還會另外算一次開車時間，開車時間
如果很短（10 分鐘以內）還會多查一次走路時間（見上面「刷新按鈕」那節的表格）。這些都
屬於 Google Maps Platform 比較貴的等級（Places 的「Contact Data」、Directions API），
但都是「每個地點/每對相鄰地點只查一次、查過就永久快取」，不是每次開網頁都重查，走路
時間也只在開車時間夠短時才會多查那一次（多數城市/景點之間動輒好幾公里，根本不會觸發），
所以順便把費用/用量怎麼查整理在這裡。⚠️ **口袋清單同步 My Maps KML 這個動作本身完全
免費、不算 Google Maps Platform 的費用**——那是直接用瀏覽器抓 Google 提供的公開匯出
檔案，不需要 API 金鑰，跟這裡講的計費完全無關。

**大概要 access 多少次才會開始收費**：
Google Maps Platform 從 2025 年 3 月改了計費方式：不再是「每月 $200 額度」，而是**每個
API 各自有自己的每月免費次數**（Autocomplete 這種基本資料 SKU 免費額度比較高；
`opening_hours` 屬於比較貴的「Contact Data」等級，免費額度比較低，但仍是「每月幾千次」
等級，不是個位數）。以兩人規劃一次 7 天行程、頂多重新搜尋/編輯幾十個地點來說，觸發的
次數是幾十到最多一兩百次，跟免費額度比是很小的量，正常使用不太可能碰到。⚠️ 這是憑印象
講的大致範圍，不是即時查證的精確數字（Google 的定價頁面會調整），需要精確數字時要直接
去 Cloud Console 看專案當下實際適用的價目表。

**去哪裡看即時（非月結）用量**：在 **Google Cloud Console**（console.cloud.google.com，
用 Firebase 這個 Blaze 專案登入）：
- 左側選單「**APIs & Services**」→「**Google Maps Platform**」→「**Metrics**」：可以看到
  Places/Maps 各個 API 每天被呼叫幾次，更新大概是幾小時內的延遲，不是月結
- 左側選單「**Billing**」→「**Reports**」：看實際花費金額，一樣是抓最近幾小時到一天內的
  資料畫成圖表，可以照 API/服務別拆開看
- 想知道「現在每 1000 次查詢大概多少錢」（不是實際花了多少，是費率本身）：
  「**Billing**」→「**Pricing**」（或 Google Maps Platform 的官方 Pricing 頁面），查
  Places API「Contact Data」、Directions API 目前各自的費率——Google 偶爾會調整，上面
  講的「幾千次免費額度」是憑印象講的大致範圍，要精確數字以這裡查到的當下頁面為準
- 「花費超過 $1 提醒」的**主動通知**設定之前失敗過（見 `願望清單.md` 第 1 項，
  `gcloud billing budgets create` 一直回傳 400 錯誤，原因沒查出來），跟這裡的「查詢當前
  用量」是兩件事——沒有主動通知的話，養成偶爾去 Metrics 頁面看一眼的習慣即可

## 技術備註

- 純靜態單一 HTML 檔（`kyushu-itinerary.html`），無框架、無建置流程
- 地圖、地點搜尋用 Google Maps JavaScript API + Places API（金鑰限定 GitHub 網域，本機
  打開網頁時地圖/搜尋功能無法使用，只能在正式網址上測試）
- 拖曳排序（行程、口袋清單、待辦、打包）都是自己刻的，用 Pointer Events 手機/電腦通用，
  沒有依賴外部套件
- 公開版（`github-site/index.html`）會自動把訂位代號遮蔽成 `ICW***`，本機版才有完整代號
- 還沒做到、做不到、或先擱置的功能整理在 `願望清單.md`
