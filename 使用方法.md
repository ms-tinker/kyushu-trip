# 九州親子自駕行程 App — 使用方法

> 這份檔案是整個 app 的說明文件。之後每次有功能更新、部署，都會一併更新這裡的內容，
> 讓你隨時能回來看目前有哪些功能、怎麼用。

## 這是什麼

給 8/26–9/1 2026 九州親子自駕（5 個月大寶寶）用的行程規劃網站。手機瀏覽器打開即可，
不用安裝 app。兩人的手機打開同一個網址，記帳、清單、行程編輯都會即時同步。

**網址**：https://ms-tinker.github.io/kyushu-trip/

## 五個分頁

### 總覽
- 快速跳轉列 + 可拖曳排序的區塊（航班、住宿、每日行程摘要、天氣、地圖、雲端空間用量）
- 整趟行程的總覽地圖，會隨著「行程」分頁裡的編輯即時更新

### 行程
- 用日期切換每一天，日期列會鎖在畫面上方，往下滑不會消失
- 每天分成「起點／早餐／上午／午餐／下午／晚餐／晚上／終點」幾個時段
- 每個地點可以編輯、刪除、新增，也能上傳照片（兩人都能傳）
- 地點卡片右上角有拖曳排序的抓取圖示，可以直接拖到同一天的其他時段；跨天搬移的話，
  在編輯表單裡選好「搬到其他天／時段」再按搬移
- 每個地點都有導航連結（會自動帶入上一站當起點）、Mapcode 查詢連結、經緯度
- 每天下方有小地圖，畫出當天路線

### 記帳
- 「新增」：日期（預設抓今天，不在行程範圍內就先選 8/26）→ 類別 → 金額（支援加減乘除）
  → 幣別 → 支付方式，類別和支付方式都有手繪風格的彩色小 icon
- 匯率會自動抓當天最新的 JPY→TWD、JPY→CNY 匯率，也能手動覆蓋；每筆紀錄都存了當時用的匯率
- 「明細」：依日期分組，每組再依類別排序（類別排序會跟著「新增」表單目前的分類順序走）；
  金額同時顯示 TWD 和 CNY
- 明細上方有總覽卡片：目前總花費（TWD + CNY）、依類別圓餅圖、依日期長條圖
- 類別、支付方式都能自己新增/排序/刪除（右上角「排序／刪除」）

### 清單
- 「待辦事項」「打包清單」兩個子分頁，都是依分類分組的可收合清單（點分類名稱展開/收合，
  也有「全部展開」「全部收合」按鈕），分類順序跟著新增表單走
- 打包清單額外有數量（跟著品項名稱一起顯示，例如「奶瓶 x4」）跟裝箱功能，箱子名稱可以自訂
- 分類（含圖示）都能自己新增/排序/刪除

### 連結
- 開車、查地點會用到的外部工具（例如 Mapcode 查詢），可以自己新增／編輯／刪除

## 資料存在哪裡

- 記帳、清單、行程編輯、照片、常用連結：存在 Firebase（Firestore + Storage），
  兩支手機即時同步，不是存在瀏覽器本機
- 總覽頁面有一個雲端空間用量的進度條，可以自己設定提醒的門檻

## 機票／住宿／每日行程怎麼更新

這三塊的基礎資料整理在同一層目錄的 `機票住宿行程.md`。以後要改班機時間、換飯店、調整
行程，直接改那份 md 檔，改完跟 Claude 說一聲「同步到 GitHub」，就會幫忙把內容套進
`kyushu-itinerary.html` 並發布。

## 技術備註

- 純靜態單一 HTML 檔（`kyushu-itinerary.html`），無框架、無建置流程
- 地圖、地點搜尋用 Google Maps JavaScript API + Places API
- 公開版（`github-site/index.html`）會自動把訂位代號遮蔽成 `ICW***`，本機版才有完整代號
